@~/.claude/ACCESSIBILITY.md

# 나이스 그리드 낭독 비교

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 목적

4세대 나이스 그리드의 칸 이름(aria-label) 문자열 합성 문제를 사업단과 자문위원에게 설명하기 위한 비교 페이지. 「현재 나이스」는 교육용 서버 실측 문자열을 그대로 재현하고, 「개선안」은 그 문자열을 지우고 표준 ARIA만 남긴다. GitHub Pages에 배포한다.

## 구조

- `index.html`: 페이지 골격. 화면 선택, 버전 전환, 그리드, 낭독 패널, 차이 요약 표
- `app.js`: 렌더링, 로빙 tabindex 키보드 탐색, 버전 전환 시 포커스 유지, 낭독 패널, Web Speech
- `compose.js`: 이름 합성 규칙(순수 함수). 실측 문자열이 곧 사양
- `data.js`: 화면 3종 데이터. 계정 ID는 가린 값
- `tests/compose.test.mjs`: `node --test tests/compose.test.mjs`

빌드 도구와 의존성은 없다. 브라우저에서 ES 모듈로 바로 실행된다.

## 규칙

- 접근성 정본은 글로벌 접근성 헌장이다. 이 저장소 고유 사항만 여기 둔다.
- 「현재 나이스」 버전은 실측과 다르게 고치지 않는다. 결함(이중 공백, 「서답형」 오기재, 「link」 문자열)도 재현 대상이다. 새 실측이 나오면 `compose.js` 테스트에 먼저 문자열을 추가한다.
- 「개선안」은 문자열 삭제와 표준 속성 보완만 한다. 새 정보를 더하지 않는다.
- 공개 저장소다. 교육용 서버 계정, 실제 사용자 ID, 내부 문서 경로를 넣지 않는다.
- em dash를 쓰지 않는다. UI 라벨에 이모지를 쓰지 않는다.
